{% extends "::admin.html.twig" %}

{% block headTitle %}Lessons{% endblock %}

{% block bodyTitle %}<h1>Lessons</h1>{% endblock %}

{% block content %}
<div class="row-fluid">
    <div class="span12">
        <p class="lead">A Lesson is a detailed representation of the Articulate Training Lesson.</p>
        <hr />
    </div>
</div>
<div class="row-fluid">
    <div class="span7">
        <hgroup>
            <h2>Lesson List</h2>
            <p>Every Articulate Training Lesson uploaded to the application should be represented in the form of a Lesson. Below is the list of all the Lessons in the system. Upload a new Articulate Training Lesson to the server and then click on the Create a New Lesson button.</p>
        </hgroup>
        <table class="table table-striped table-hover table-condensed table-bordered">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Description</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for entity in entities %}
                <tr>
                    <td><a href="{{ path('lesson_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                    <td>{{ entity.description }}</td>
                    <td>
                        <a href="{{ path('lesson_show', { 'id': entity.id }) }}"><i class="icon-eye-open"></i> Details</a>
                        <br />
                        <a href="{{ path('lesson_edit', { 'id': entity.id }) }}"><i class="icon-wrench"></i> Edit</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        <p><a class="btn btn-primary" href="{{ path('lesson_new') }}"><i class="icon-leaf icon-white"></i> Create a New Lesson</a></p>
        <p><a href="{{ path('dashboard') }}"><i class="icon-backward"></i> Back to the Dashboard</a></p>
    </div>
    <div class="span5">
        <section>
            <header><h2>Generic or Custom?</h2></header>
            <div class="well">
                <p>I'd recommend creating a custom Lessons for Companies. If a Company requests a change to their Lesson then you only need to change the Company's Lesson.</p>
                <p>Generic Lesson are great for Users who aren't associated with a Company.</p>
                <p>Remember, the same Lesson can be part of multiple Lesson Plans.</p>
            </div>
        </section>
    </div>
</div>
{% endblock %}