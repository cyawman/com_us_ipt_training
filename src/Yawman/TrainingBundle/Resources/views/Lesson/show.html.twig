{% extends "::admin.html.twig" %}

{% block headTitle %}Lesson{% endblock %}

{% block bodyTitle %}{{ entity.name }}{% endblock %}

{% block content %}
<div class="row-fluid">
    <div class="span12">
        <p class="lead">A Lesson is a detailed representation of the Articulate Training Lesson.</p>
        <hr />
    </div>
</div>

<div class="row-fluid">
    <div class="span7">
        <h2>Overview</h2>
        <p>When a Lesson is created it automatically generates an ID. The ID can never be changed. You can change the Name and Description of a Lesson as many times as you want.</p>
        <table class="table table-bordered table-striped table-hover">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ entity.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ entity.name }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ entity.description }}</td>
                </tr>
            </tbody>
        </table>
        <p><a class="btn btn-primary" href="{{ path('lesson_edit', { 'id': entity.id }) }}"><i class="icon-wrench icon-white"></i> Edit</a></p>
        <p><a href="{{ path('lesson') }}"><i class="icon-backward"></i> Back to the list</a></p>
    </div>
    <div class="span5">
        <h2>Deleting a Lesson?</h2>
        <div class="well">
            <p>Deleting a Lesson will remove the Lesson from all Lesson Plans.</p>
            <p>It does not delete the Articulate Training Lesson from the server.</p>
            <form action="{{ path('lesson_delete', { 'id': entity.id }) }}" method="post">
                {{ form_widget(delete_form) }}
                <button class="btn btn-danger" type="submit"><i class="icon-minus-sign icon-white"></i> Delete</button>
            </form>
        </div>
    </div>
</div>
{% endblock %}