{% extends "::admin.html.twig" %}

{% block headTitle %}Lesson Plans{% endblock %}

{% block bodyTitle %}Lesson Plans{% endblock %}

{% block content %}
<div class="row-fluid">
    <div class="span12">
        <p class="lead">Lesson Plans are a collection of Lessons.</p>
        <hr />
    </div>
</div>
<div class="row-fluid">
    <div class="span7">
        <hgroup>
            <h2>Lesson Plan List</h2>
            <p>Create a custom Lesson Plan to meet a particular Company's needs. Create generic Lesson Plans and share them with many Users and Companies.</p>
        </hgroup>
        <table class="table table-striped table-hover table-condensed table-bordered">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for entity in entities %}
                <tr>
                    <td><a href="{{ path('lessonplan_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                    <td>
                        <a href="{{ path('lessonplan_show', { 'id': entity.id }) }}"><i class="icon-eye-open"></i> Details</a>
                        <br />
                        <a href="{{ path('lessonplan_edit', { 'id': entity.id }) }}"><i class="icon-wrench"></i> Edit</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        <p><a class="btn btn-primary" href="{{ path('lessonplan_new') }}"><i class="icon-tasks icon-white"></i> Create a New Lesson Plan</a></p>
        <p><a href="{{ path('dashboard') }}"><i class="icon-backward"></i> Back to the Dashboard</a></p>
    </div>
    <div class="span5">
        <section>
            <header><h2>Generic or Custom?</h2></header>
            <div class="well">
                <p>I'd recommend creating a custom Lesson Plan when you are dealing with a Company. If a Company requests a change to their Lesson Plan then you only need to change the Company's Lesson Plan.</p>
                <p>Generic Lesson Plans are great for Users who aren't associated with a Company.</p>
                <p>Remember, the same Lesson can be part of multiple Lesson Plans.</p>
            </div>
        </section>
    </div>
</div>
{% endblock %}