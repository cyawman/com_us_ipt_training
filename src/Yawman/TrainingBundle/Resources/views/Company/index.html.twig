{% extends "::admin.html.twig" %}

{% block headTitle %}Company List{% endblock %}

{% block bodyTitle %}Companies{% endblock %}

{% block content %}
<div class="row-fluid">
    <div class="span12">
        <p class="lead">A listing of all the companies in the system. Each Company is assigned a group of Users and Lesson Plans.</p>
        <hr />
    </div>
</div>
<div class="row-fluid">
    <div class="span7">
        <h2>Company List</h2>
        <p>Checkout the Company Dashboard to view its recent User activity. You can see all Users and Lesson Plans in the Company by clicking on the Details link. Click on Edit to change the name of the Company.</p>
        <table class="table table-striped table-hover table-condensed table-bordered">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for entity in entities %}
                <tr>
                    <td><a href="{{ path('company_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                    <td>
                        <a href="{{ path('company_dashboard', { 'id': entity.id }) }}"><i class="icon-list-alt"></i> Dashboard</a><br />
                        <a href="{{ path('company_show', { 'id': entity.id }) }}"><i class="icon-eye-open"></i> Details</a><br />
                        <a href="{{ path('company_edit', { 'id': entity.id }) }}"><i class="icon-wrench"></i> Edit</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        <a class="btn btn-primary" href="{{ path('company_new') }}"><i class="icon-briefcase icon-white"></i> Create a New Company</a>
    </div>
    <div class="span5">
        <section>
            <header><h2>Single Users</h2></header>
            <div class="well">
                <p>You don't need to create a Company if you want to give a single User access. This is useful if you want to create an account for a potential customer. You can give them access to a demo Lesson Plan for example.</p>
            </div>
        </section>
    </div>
</div>
{% endblock %}